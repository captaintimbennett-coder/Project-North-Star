# Shared Library

This directory is reserved for shared utilities, validation, formatting,
repositories, and external service clients.

Do not use it as a miscellaneous dumping ground. Add a module only when it has a
clear cross-feature responsibility.
